export function UserName(){
	// TODO -> pegar o nome do gerenciador de estado global
	const username = "Lucas Weber"
	return(
		<span className="text-xs text-custom-black font-semibold">{username}</span>
	);
}